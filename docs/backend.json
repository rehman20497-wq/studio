
{
  "entities": {
    "Provider": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Provider",
      "type": "object",
      "description": "Represents a service provider in the network.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Provider entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the provider."
        },
        "slug": {
          "type": "string",
          "description": "A URL-friendly slug for the provider."
        },
        "logoUrl": {
          "type": "string",
          "description": "URL of the provider's logo.",
          "format": "uri"
        },
        "bannerImageUrl": {
          "type": "string",
          "description": "URL of the provider's banner image.",
          "format": "uri"
        },
        "description": {
          "type": "string",
          "description": "A rich-text description of the provider."
        },
        "solutions": {
          "type": "array",
          "description": "An array of solutions offered by the provider.",
          "items": {
            "type": "string",
            "enum": [
              "Cloud Solutions",
              "Communications",
              "AI Solutions",
              "Connectivity"
            ]
          }
        },
        "impressions": {
          "type": "number",
          "description": "Total number of times the provider has been viewed."
        },
        "clicks": {
          "type": "number",
          "description": "Total number of times the provider link has been clicked."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the provider was created."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the provider was last updated."
        },
        "published": {
          "type": "boolean",
          "description": "Indicates if the provider is visible to the public."
        }
      },
      "required": [
        "id",
        "name",
        "slug",
        "logoUrl",
        "description",
        "solutions",
        "createdAt",
        "updatedAt"
      ]
    },
    "ReviewerProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ReviewerProfile",
      "type": "object",
      "description": "Represents a reviewer's profile and associated information for display on the USA Testimonial Network.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ReviewerProfile entity."
        },
        "reviewerName": {
          "type": "string",
          "description": "The name of the reviewer."
        },
        "state": {
          "type": "string",
          "description": "The U.S. state the reviewer is from."
        },
        "profilePictureUrl": {
          "type": "string",
          "description": "URL of the reviewer's profile picture.",
          "format": "uri"
        },
        "reviewId": {
          "type": "string",
          "description": "Reference to the Review entity. (Relationship: Review 1:1 ReviewerProfile)"
        }
      },
      "required": [
        "id",
        "reviewerName",
        "state",
        "profilePictureUrl",
        "reviewId"
      ]
    },
    "Review": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Review",
      "type": "object",
      "description": "Represents a user review.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Review entity."
        },
        "reviewText": {
          "type": "string",
          "description": "The text content of the review."
        }
      },
      "required": [
        "id",
        "reviewText"
      ]
    },
    "AnimationSequence": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AnimationSequence",
      "type": "object",
      "description": "Represents a sequence of reviewer profiles for animation.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the AnimationSequence entity."
        },
        "profileIds": {
          "type": "array",
          "description": "References to ReviewerProfiles. (Relationship: AnimationSequence 1:N ReviewerProfile)",
          "items": {
            "type": "string"
          }
        },
        "sequenceOrder": {
          "type": "number",
          "description": "The order of the animation sequence."
        }
      },
      "required": [
        "id",
        "profileIds",
        "sequenceOrder"
      ]
    },
    "BlogPost": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "BlogPost",
      "type": "object",
      "description": "Represents a blog post in the admin panel.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the BlogPost entity."
        },
        "title": {
          "type": "string",
          "description": "The title of the blog post."
        },
        "category": {
          "type": "string",
          "description": "The category of the blog post."
        },
        "featuredImageUrl": {
          "type": "string",
          "description": "URL of the post's featured image.",
          "format": "uri"
        },
        "content": {
          "type": "string",
          "description": "The rich-text content of the blog post."
        },
        "quote": {
          "type": "string",
          "description": "An optional pull-quote from the content."
        },
        "authorName": {
          "type": "string",
          "description": "The name of the author."
        },
        "authorImageUrl": {
          "type": "string",
          "description": "URL for the author's profile image.",
          "format": "uri"
        },
        "views": {
          "type": "number",
          "description": "Total number of times the blog post has been viewed."
        },
        "comments": {
          "type": "number",
          "description": "Total number of comments on the post."
        },
        "shares": {
          "type": "number",
          "description": "Total number of times the post has been shared."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the post was created."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the post was last updated."
        }
      },
      "required": [
        "id",
        "title",
        "category",
        "featuredImageUrl",
        "content",
        "authorName",
        "authorImageUrl",
        "createdAt",
        "updatedAt"
      ]
    },
    "NewsletterSubscriber": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "NewsletterSubscriber",
      "type": "object",
      "description": "Represents a user who has subscribed to the newsletter.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the subscriber."
        },
        "email": {
          "type": "string",
          "description": "The email address of the subscriber.",
          "format": "email"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the subscription was created."
        }
      },
      "required": ["id", "email", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/providers/{providerId}",
        "definition": {
          "entityName": "Provider",
          "schema": {
            "$ref": "#/entities/Provider"
          },
          "description": "Stores information about each service provider.",
          "params": [
            {
              "name": "providerId",
              "description": "The unique identifier for the provider."
            }
          ]
        }
      },
      {
        "path": "/reviewer_profiles/{profileId}",
        "definition": {
          "entityName": "ReviewerProfile",
          "schema": {
            "$ref": "#/entities/ReviewerProfile"
          },
          "description": "Stores reviewer profile information, including reviewerName, state, profilePictureUrl, and reviewId.  'profileId' is the unique identifier for each reviewer profile.",
          "params": [
            {
              "name": "profileId",
              "description": "The unique identifier for the reviewer profile."
            }
          ]
        }
      },
      {
        "path": "/reviews/{reviewId}",
        "definition": {
          "entityName": "Review",
          "schema": {
            "$ref": "#/entities/Review"
          },
          "description": "Stores review data, including reviewText. 'reviewId' is the unique identifier for each review.",
          "params": [
            {
              "name": "reviewId",
              "description": "The unique identifier for the review."
            }
          ]
        }
      },
      {
        "path": "/animation_sequences/{sequenceId}",
        "definition": {
          "entityName": "AnimationSequence",
          "schema": {
            "$ref": "#/entities/AnimationSequence"
          },
          "description": "Stores the sequence of reviewer profiles for animation, including profileIds and sequenceOrder. 'sequenceId' is the unique identifier for each animation sequence.",
          "params": [
            {
              "name": "sequenceId",
              "description": "The unique identifier for the animation sequence."
            }
          ]
        }
      },
      {
        "path": "/blog_posts/{postId}",
        "definition": {
          "entityName": "BlogPost",
          "schema": {
            "$ref": "#/entities/BlogPost"
          },
          "description": "Stores all blog post data, managed via the admin panel.",
          "params": [
            {
              "name": "postId",
              "description": "The unique identifier for the blog post."
            }
          ]
        }
      },
       {
        "path": "/newsletter_subscribers/{subscriberId}",
        "definition": {
          "entityName": "NewsletterSubscriber",
          "schema": {
            "$ref": "#/entities/NewsletterSubscriber"
          },
          "description": "Stores email addresses of users who subscribed to the newsletter.",
          "params": [
            {
              "name": "subscriberId",
              "description": "The unique identifier for the newsletter subscriber."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to support the USA Testimonial Network application. It focuses on optimizing data retrieval for the animated testimonial experience, ensuring scalability, and maintaining data integrity. The core entities—ReviewerProfile, Review, and AnimationSequence—are organized into collections that facilitate efficient querying and updates, especially concerning the profile placement logic and animation flow across the USA map. A new 'BlogPost' entity and '/blog_posts' collection have been added to support the blog management functionality, allowing admins to create and manage content. \n\nTo achieve Authorization Independence (CRITICAL), we do not need to denormalize any authorization context. There is no access control defined in the User Request, so all documents are treated equally. If an access control system were defined (e.g., users can only edit their reviews), the structure would be updated based on strategies outlined in the prompt.\n\nStructural Segregation: Each collection is dedicated to a specific entity, ensuring that all documents within a collection share the same security posture. This simplifies security rules and makes the application more maintainable. For example, `reviewer_profiles` contains only reviewer profile documents, and `reviews` contains only review documents. This segregation avoids mixing data with different access requirements in the same collection.\n\nAccess Modeling: The structure uses path-based ownership where applicable and relies on denormalization to avoid `get()` calls in security rules. The `reviews` collection stores review data. The `reviewer_profiles` collection stores reviewer profile data and includes a `reviewId` to link it to a specific review. The `animation_sequences` collection holds information about the sequence of reviewer profiles for the animation, including an array of `profileIds`. These structures are tailored to support the application's features, such as the animated USA map, profile placement, sequential profile display, popover reviews, and continuous transition between sets of testimonials."
  }
}

    